<?php

use Illuminate\Database\Seeder;
use App\BookHold;

class BookHoldTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // BookHold::create(['hold_status' => 'Book placed for hold']);
        // BookHold::create(['hold_status' => 'Unavailable for hold']);
        // BookHold::create(['hold_status' => 'Ready to pick up']);
    }
}
